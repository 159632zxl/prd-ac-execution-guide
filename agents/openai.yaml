interface:
  display_name: "PRD AC Execution Guide"
  short_description: "Create bounded PRDs or change packets with acceptance and code-network gates."
  default_prompt: "Create a bounded PRD or Change Packet with numbered acceptance criteria, stage packets, verified code-network contracts, task dependencies, and phase gates."
